# Photobooth
## ФОТОБУДКА

Фотобудка: Canon EDSDK + сублимационный принтер + сенсорный экран.
FastAPI backend + pywebview fullscreen window.

## Структура

```
photobooth/
  python/              ← Python 3.12 embedded + pip + все пакеты
  backend/             ← FastAPI, камера, принтер, облако
  frontend/            ← HTML/CSS/JS интерфейс
  assets/              ← шрифты, позы, frontend-медиа и карточка Яндекс.Диска
  templates/           ← шаблоны печати
  bin/                 ← ffmpeg
  EDSDK_Win/           ← Canon SDK
  app.py               ← точка входа
  requirements.txt
  requirements-win.lock ← точный состав embedded Windows runtime
```

`python/` — портативный Python со всеми зависимостями. Заменяет venv.
Создаётся автоматически (см. ниже). В git не хранится.

## Установка

### Вариант А: из релиза (рекомендуется)

```
Скачать photobooth-win.zip из GitHub Releases → распаковать в C:\photobooth\
```

ZIP содержит код и `python/` с пакетами. Готово к запуску.

### Вариант Б: из git clone

```
git clone → script_devstart.bat
```

Скрипт сам скачает embedded Python, поставит pip и пакеты.

### Результат одинаковый

Оба варианта дают идентичную структуру. Взаимозаменяемы.

## Запуск

```
Разработка:    script_devstart.bat     (fetch/reset origin/main + pip install + app.py --dev)
Продакшен:     _setup_windows.bat      (киоск-режим, автозапуск при загрузке)
```

`_setup_windows.bat` также устанавливает системную задачу
`PhotoboothResetCanonR8`. На уже настроенной будке после получения версии с
восстановлением камеры этот bat нужно один раз повторно запустить от
администратора; сам kiosk-пользователь администратором не становится.

## Локальный просмотр экранов без оборудования

Запустите Live Server от корня всего проекта `photobooth` или обычный статический
сервер из корня проекта:

```bash
python3 -m http.server 8000 --bind 127.0.0.1
```

После этого экраны доступны по адресам:

- `http://127.0.0.1:8000/frontend/index.html#idle` — стартовый экран;
- `http://127.0.0.1:8000/frontend/index.html#idle-locked` — заблокированный платный режим;
- `http://127.0.0.1:8000/frontend/index.html#shooting` — съёмка и обратный отсчёт;
- `http://127.0.0.1:8000/frontend/index.html#processing` — обработка;
- `http://127.0.0.1:8000/frontend/index.html#template` — выбор формата;
- `http://127.0.0.1:8000/frontend/index.html#template-multi` — мультивыбор нескольких отпечатков;
- `http://127.0.0.1:8000/frontend/index.html#photo-choice` — выбор одного фото и рамки;
- `http://127.0.0.1:8000/frontend/index.html#done` — печать и демонстрационный QR-код;
- `http://127.0.0.1:8000/frontend/index.html#camera-searching` — поиск камеры;
- `http://127.0.0.1:8000/frontend/index.html#no-camera` — ошибка камеры.

Hash-превью предназначено для Live Server и читает настоящие `config_app.json`
и `templates/<template_pack>/config.json`. Поэтому порядок шаблонов, их имена,
подписи, `photo_choice`, количество кадров и таймеры совпадают с боевыми
настройками. Прямой `file://` не поддерживается. Только фотографии-заглушки и
QR-ссылка остаются демонстрационными: реальные появляются лишь после проведённой
сессии. Они лежат отдельно от production-кода в `assets/dev`. Без
hash-части страница работает как раньше: подключается к backend и показывает его
состояние.

Режим определяется один раз при загрузке страницы. Изменение части после `#`
вызывает обычную перезагрузку, поэтому preview и production-состояния внутри одной
страницы никогда не смешиваются.

## Сборка релиза (GitHub Actions)

При каждом push в main:

```
GitHub Actions (windows-latest)
  → checkout репо
  → скачивает Python 3.12.10 embedded с python.org
  → ставит точные версии из requirements-win.lock без bytecode cache
  → запускает полный набор тестов
  → создаёт чистую release_stage из tracked-файлов + embedded Python
  → собирает full ZIP и app/assets/python/bin/templates/edsdk/drivers ZIP
  → публикует все ZIP как GitHub Release "latest"
  → переносит изменившиеся архивы в photobooth_system/updates на Яндекс.Диске
  → последним публикует status.json
  → сообщает результат администратору в Telegram
```

Постоянная ссылка: `Releases → latest → photobooth-win.zip`

`app`, `assets`, `python`, `bin`, `templates`, `edsdk` и `drivers` получают SHA-256
отсортированного списка относительных путей и байтов файлов. Времена файлов в
эту версию не входят. `full` сохраняет SHA-256 самого ZIP для совместимости со
старыми updater-ами. `__pycache__`, `*.pyc`, локальные данные и логи в релиз не
попадают.

Workflow использует GitHub Secrets `YADISK_TOKEN`, `TG_BOT_TOKEN` и
`TG_ADMIN_ID`. Неизменившийся папочный SHA переиспользует уже опубликованный
архив. Новый архив Яндекс.Диск до трёх раз пытается забрать по GitHub-ссылке с
паузами 2 и 4 секунды. При неудаче workflow останавливается, а прежний
`status.json` остаётся активным. На Яндекс.Диске у архивов постоянные пути:
`full.zip`, `app.zip`, `assets.zip`, `python.zip`, `bin.zip`, `templates.zip`,
`edsdk.zip` и `drivers.zip`. Изменившийся архив импортируется с перезаписью, затем последним
перезаписывается `status.json` с новыми SHA-256 и размерами. Старые архивы с SHA
в имени автоматически не удаляются: после перехода на постоянные пути их можно
один раз удалить вручную. Более старая параллельная сборка отменяется.

## Обновление

```
Push в main
  → GitHub Actions собирает full и семь компонентных архивов
  → публикует только изменившиеся папочные версии на Яндекс.Диск
  → GitHub Actions последним обновляет status.json
  → Telegram сообщает об успешной публикации
  → администратор отправляет /restart

Запуск app.py
  → читает status.json с Яндекс Диска
  → читает JSON mapping из .update_hash
  → сравнивает app/assets/python/bin/templates/edsdk/drivers по отдельности
  → без валидного mapping скачивает один full ZIP
  → с mapping скачивает только изменившиеся папочные ZIP
  → показывает на экране процент, объём и скорость скачивания
  → при временной сетевой ошибке до пяти раз получает новую ссылку и повторяет скачивание (2/4/8/16 с)
  → проверяет full по размеру + SHA-256, компоненты по размеру + CRC ZIP
  → распаковывает все скачанные ZIP в одну stage-папку
  → заменяет выбранные папки и сохраняет `.env`, фотографии, очередь и логи
  → атомарно записывает новый JSON в .update_hash и перезапускается
```

`/update` сохранён как ручной резервный способ повторно опубликовать последний
GitHub Release через VPS, но для обычного push в `main` больше не требуется.
После выхода будки релиз применяет одноразовый PowerShell-скрипт, поэтому он может заменить
заблокированные `python.exe`, DLL и PYD без постоянного launcher-сервиса.
Одинаковый updater работает и в киоске, и при `script_devstart.bat`: аргумент
`--dev` сохраняется при автоматическом перезапуске.

Файл `.update_in_progress.json` захватывается ещё до сетевой проверки, поэтому
второй запуск не начинает параллельное скачивание и не доходит до backend/EDSDK.
Пока экран обновления открыт, приложение проверяет и распаковывает все ZIP во
временную папку. Одноразовый установщик ждёт полного выхода embedded Python,
заменяет подготовленные верхнеуровневые entries и только после успеха атомарно
записывает `.update_hash`. Перед заменой существующие entries переносятся во
временный backup; при ошибке установщик возвращает уже затронутые entries назад.
Затем приложение открывается в том же режиме. Этапы и ошибки записываются в
`photobooth.log`.

`.update_hash` теперь содержит один JSON object с ключами `full`, `app`,
`assets`, `python`, `bin`, `templates`, `edsdk`, `drivers`. Отсутствующий, повреждённый или
удалённый файл означает чистую установку и приводит к full update. Старый
валидный mapping с неполным набором известных компонентов догружает только
отсутствующие компоненты; это позволяет добавить `assets` без принудительного
full update. Старый 64-символьный full hash, совпадающий с текущим `status.json`,
автоматически превращается в новый mapping без повторного скачивания.

При первом переходе с версии updater-а, которая ещё не знает компонент
`assets`, безопаснее удалить старый `.update_hash` перед `/restart`: старая
версия тогда штатно установит `full`, содержащий новую корневую папку. Если
файл уже отсутствует или повреждён, дополнительных действий не требуется.

`status.json` содержит `path`, `size`, `sha256` и `hash_type` каждого архива в
общем `artifacts`. У `full` стоит `hash_type: "zip"`, и `sha256` относится к
самому ZIP. У остальных стоит `hash_type: "folder"`, и `sha256` относится к
отсортированным путям и байтам файлов папки без времён файлов. Единое имя
`sha256` сохранено для уже установленных updater-ов; дублирующих полей
`zip_sha256` и `folder_sha256` нет. Поля `schema_version: 1` и `active: "full"`
тоже сохранены для старого формата.

Будке доступ к GitHub не нужен: GitHub Actions публикует обновление на
Яндекс.Диск, а будка всегда скачивает его только оттуда.

На текущем этапе `config_app.json` и `config_camera.json` управляются из
репозитория и заменяются версиями из каждого релиза. Локальными остаются `.env`,
`photos`, `yadisk_queue.json` и логи. Если конфиги позднее будут вынесены из
репозитория, их нужно будет снова добавить в preserve-список updater-а.

## Печать и шаблоны

Одна сессия снимает `num_photos` кадров из `config_app.json`. Рабочая
конфигурация снимает четыре кадра, после чего пользователь выбирает:

- `grid` — четыре кадра на одном горизонтальном листе 4x6;
- `single` — пользователь выбирает один из четырёх кадров; с рамкой он занимает
  всю область четырёх grid-слотов, без рамки кадр через центральный `cover`
  заполняет весь печатный растр;
- `strips` — две одинаковые физические полоски сразу размещаются в верхней и
  нижней половинах горизонтального листа 4x6; очередь с `2inch cut` разрезает
  его после печати.

Готовый JPEG имеет точный high-quality raster RX1HS: `3688x2480` с меткой
`600x600 DPI`. Драйвер преобразует его в нативный режим принтера `300x600`.
Фоны grid и strips оба хранятся как полные горизонтальные листы
`3688x2480`. В strips восемь слотов явно записаны в `print_layout`: четыре
исходных кадра повторяются во второй физической половине листа через
`photo_index`. Приложение не дублирует и не поворачивает готовый печатный лист.

`print_size`, справочный `print_trim`, единый `photo_size_px` и все координаты,
индексы и повороты `print_layout` находятся в
`templates/<template_pack>/config.json`. Обязательный `background` и опциональный
`foreground` обязаны точно совпадать с `print_size`; неправильный размер
отклоняется, чтобы скрытый fallback не изменил геометрию печати. `foreground`
должен иметь alpha-канал и накладывается последним: `background → фотографии →
foreground`. Если верхний слой конкретному шаблону не нужен, поле просто
отсутствует. Каждый кадр целиком и пропорционально вписывается по центру своего
слота: приложение не обрезает фотографию, а при несовпадении пропорций оставляет
видимым свободный фон шаблона вокруг неё.
Поле `label` задаёт подпись варианта на экране. Новые элементы в `templates`
появляются в выборе автоматически — отдельных кнопок во frontend нет.

### Мультивыбор: несколько отпечатков за одну сессию

Большой компании иногда нужно не два, а четыре стрипа, или две открытки плюс
одно фото. Для этого на экране выбора формата есть третья круглая кнопка
`НЕСКОЛЬКО ОТПЕЧАТКОВ` — между лупой слева и крестиком справа. Она появляется
только когда активен технический event `technical_event_name` (сейчас `Кафе`) и
`multi_print_enabled=true`. На свадьбах и других гостевых event её нет вообще, и
экран работает точно как раньше: один тап — один лист.

В мультирежиме по центру каждой миниатюры появляется бейдж со счётчиком `0` и
кнопками `−` и `+`. Сам тайл при этом становится инертным: случайный тап по
превью уже ничего не печатает, счётчик меняют только `−` и `+`. Тайл
`photo_choice` остаётся раскрывающимся, а его бейдж показывает только сумму —
конкретные кадры набираются внутри раскрытой панели, отдельно для версий
`С РАМКОЙ` и `БЕЗ РАМКИ`. В правом нижнем углу появляется кнопка `ПЕЧАТАТЬ N`,
заблокированная пока корзина пуста.

Единица корзины — физический лист 4x6, а не полоска. Один `strips` это уже две
полоски, поэтому «четыре полоски» это `strips` со счётчиком `2`.
`multi_print_max_sheets` (сейчас 6) ограничивает суммарное число листов во всей
корзине, а не в одном тайле; допустимый диапазон `1..20`, непригодное значение
откатывается к 6 с записью в лог.

Backend получает корзину одним сообщением `select_template` с массивом `items`;
одиночный тап остаётся прежним сообщением с полями `template`, `photo_index` и
`with_frame`. Каждый элемент проходит те же проверки, что и одиночный выбор, плюс
`copies`. Одна некорректная позиция отклоняет всю корзину целиком: напечатать
часть выбора означало бы взять с гостя деньги за листы, которых он не
подтверждал. Корзина за пределами лимита и корзина в гостевом event отклоняются
так же, и гость может выбрать заново.

Одинаковые позиции сливаются по ключу «шаблон + номер кадра + рамка», поэтому
каждая раскладка компонуется ровно один раз, а готовый JPEG ставится в очередь
столько раз, сколько нужно копий. Очередь принтера последовательная, а имя
Windows-очереди по-прежнему выбирается по шаблону каждого задания — смешанная
корзина grid+strips сама уходит в основную и режущую очереди без дополнительной
настройки.

Таймаут `template_select_timeout` не печатает набранную корзину: она попадает на
будку только по нажатию `ПЕЧАТАТЬ N`. Гость, ушедший от экрана, получает один
лист `default_template`, как и до появления мультивыбора. Выход из мультирежима
очищает корзину, чтобы скрытый выбор не мог напечататься позже.

Разрешённая `/unblock`-сессия списывается по-прежнему одна на сессию, независимо
от числа листов в корзине: оператор в кафе всё равно считает деньги вручную.
Экран печати при корзине больше одного листа показывает `Печатаем N листов…`,
иначе гость уйдёт после первого листа.

Локально режим можно посмотреть по адресу
`http://127.0.0.1:8000/frontend/index.html#template-multi`.

После получения всех кадров будка один раз показывает экран «Обрабатываем
фото…». Четыре оригинала уменьшаются один раз и только в памяти, после чего из
них собираются реальные экранные превью всех настроенных вариантов. Шаблон с
`photo_choice: true` получает по одной рамочной версии для каждого кадра и по
одной безрамочной версии с тем же `cover`, который используется при печати.
Верхний тайл такого шаблона циклически показывает рамочные версии, а после
нажатия раскрывает четыре кадра и сегмент «С рамкой / Без рамки».

`template_select_timeout` (сейчас 60 секунд) не жёсткий: любое касание в любом
месте экрана выбора заново отсчитывает полные 60 секунд. Отдельной кнопки
продления нет — работают и нажатия по шаблонам, и тапы по пустому фону.
Frontend отправляет `template_activity`, backend сдвигает свой дедлайн и
подтверждает это сообщением `template_timer`, по которому экранный отсчёт
«Осталось N секунд» стартует заново. Дефолтный шаблон выбирается только если
гость не касался экрана целые 60 секунд.

Уменьшенные
фото ограничены `720 px` по длинной стороне: это больше крупнейшего слота текущих
превью и не требует апскейла, но позволяет JPEG-декодеру Canon сразу работать
примерно в масштабе 1/8 по каждой стороне. Готовые превью имеют размер
`720x484`. Уменьшенные фоны кешируются рядом с оригиналами как
`*_preview.jpg`, а прозрачные foreground-слои — как `*_preview.png`. PNG
уменьшается с premultiplied alpha, чтобы на полупрозрачных краях не появлялись
тёмные ореолы. При каждой сессии проверяются размер и время изменения исходника,
поэтому новый или заменённый слой пересоздаётся автоматически. Итоговые превью
сессии временно лежат только в `photos/<session>/previews`, удаляются сразу после
выбора (и при ошибке/новом запуске приложения), а полноразмерный макет собирается
только для выбранного шаблона. Ни кеши слоёв, ни превью сессии не входят в
печатную очередь, Яндекс Диск, манифест или Telegram.

При `print_enabled=true` готовый JPEG передаётся встроенному Windows image
print handler, а тот — установленному DNP-драйверу. Приложение не рисует
изображение в printer DC и не переписывает `DEVMODE`: именно этот прямой GDI
путь на RX1 давал формально завершённые задания с белыми листами.
`printer_name` задаёт точное имя принтера; пустое значение использует принтер
по умолчанию пользователя `Photobooth`. Для стрипов можно указать
`printer_name_strips` — отдельную очередь того же DNP-драйвера, в которой включён
фирменный `2inch cut`. Если поле пустое, обе раскладки печатаются через основную
очередь, а две полоски нужно разрезать вручную.

Административные `/print_queue` и `/clear_print_queue` используют именно эти
два имени и работают с Windows spooler, а не с файлами в `photos` или
`photos_print_jobs`. Очистка сначала запрашивает общий purge очереди. Если
киоск-пользователю разрешено управлять только собственными заданиями, будка
пробует удалить их по одному и явно сообщает, если какие-либо задания остались.
Операция синхронизирована с передачей нового JPEG в Windows, поэтому числа до
и после очистки не снимаются посреди добавления задания. Новые задания,
поставленные приложением уже после команды, снова появятся в spooler как обычно.

Telegram print jobs сохраняют исходник и подготовленный горизонтальный JPEG в
`photos_print_jobs/<job_id>/original.<ext>` и `print_4x6.jpg`. Эта runtime-папка
лежит рядом с `photos/`, добавлена в `.gitignore` и не попадает в релизы. Параметр
`keep_custom_print_files` управляет их хранением после попытки печати. При
`true` оба файла остаются для проверки; при `false` они удаляются после успеха
или ошибки драйвера.

Каждое Telegram-задание обязано содержать режим `print_mode`: `fit` целиком
вписывает снимок по центру и оставляет белые поля, а `fill` заполняет весь лист
с центральной обрезкой лишнего. Будка повторно декодирует оригинал по содержимому
и учитывает EXIF-поворот. Горизонтальный исходник обрабатывается сразу;
вертикальный и квадратный сначала поворачиваются на 90° против часовой стрелки.
В обоих режимах итог для Windows всегда один горизонтальный JPEG размером
`3688×2480`, поэтому настройки очереди принтера от ориентации исходника не
меняются. Этот результат должен в точности соответствовать варианту, который
пользователь выбрал в Telegram-превью.

Для проверки реальной геометрии есть
`diagnostics/print_alignment_3688x2480.png`. Его нужно отправить боту именно как
документ, чтобы Telegram не пережал исходник. Соотношение уже точное, поэтому
задание уйдёт сразу без кнопок. Цветные полосы от каждого края имеют ширину
`20 px`; белый пунктир показывает текущий внешний отступ grid `61 px`, чёрный —
reference `120 px` из режима драйвера Border=Enable. `Border=Disable` отключает
эту искусственную рамку драйвера, но не физический bleed borderless-печати:
растр DNP покрывает `156,1×105,0 мм`, а готовый лист имеет `152×101 мм`, поэтому
теоретически теряется примерно `48 px` с каждой стороны. Фактический тест этого
экземпляра дал `40 px` слева, `50 px` сверху и по `55 px` справа и снизу.

Эти четыре значения записаны в `print_trim` template-конфига; вложенный
`visible_size: [3593, 2375]` явно фиксирует итоговую видимую область. Полный
отправляемый принтеру растр всегда остаётся `3688×2480 px`. У каждого шаблона
`photo_size_px` хранит единый размер фотослота, а `print_layout` — единственный
набор слоёв, координат, индексов и поворотов. Compositor сразу вставляет
исходники в эти места полного растра, затем накладывает `foreground` и не
уменьшает повторно весь готовый макет. Шаблон может использовать не все
снятые в сессии кадры. Для `photo_choice` он обязан ссылаться только на
`photo_index: 0`: backend перед композицией передаёт на это место выбранный
полноразмерный оригинал сессии.

Выбор `single` отправляет `photo_index` и boolean `with_frame`. Backend строго
проверяет оба поля. При `with_frame=true` используется обычная композиция
`background → выбранное фото → foreground`; при `false` template-слои вообще
не открываются, а выбранный оригинал с учётом EXIF-ориентации центрированно
заполняет полный `print_size` через `cover`.

Экранное превью использует тот же `print_layout`, но не применяет `print_trim`:
фон уменьшается до preview-размера, а координаты, размеры слотов и фотографии
умножаются на тот же коэффициент. Поэтому второго набора размеров или координат
для превью нет. Опциональный `foreground` уменьшается и накладывается после
фотографий, до экранного поворота или разделения листа. Для strips уменьшенный
лист разрезается горизонтально пополам, обе половины поворачиваются по
`preview_rotation: cw` и показываются отдельными вертикальными полосками с
промежутком; grid и single используют `preview_split: none` и `preview_rotation: none`.
После реальной резки внешние поля получаются
симметричными, а граница двух strips остаётся на физической середине для
`2inch cut`. Центральные квадрат и круг диагностики имеют размер `600×600 px`,
то есть ровно один дюйм при 600 DPI. Исходник можно пересоздать командой
`python diagnostics/generate_print_alignment.py`.

Вторую очередь создаёт `_setup_strips_printer.bat`. Сначала нужно установить и
проверить основную очередь, затем запустить батник на Windows: он запросит права
администратора, прочитает `printer_name` и `printer_name_strips` из
`config_app.json` и подключит вторую очередь к тому же драйверу и физическому
порту. Повторный запуск безопасен: совпадающая очередь не пересоздаётся, а при
конфликте драйвера или порта скрипт остановится, ничего не удаляя.

После создания откроются свойства новой очереди. На вкладке **Advanced** нужно
открыть **Printing Defaults**, включить фирменный параметр DNP `2inch cut` и
нажать Apply/OK. Стандартный Windows `Add-Printer` не умеет задавать private
DEVMODE-параметры DNP автоматически; Windows image print handler использует
сохранённые defaults выбранной очереди целиком.

Размер бумаги 4x6, borderless, качество, одна копия и остальные параметры DNP
нужно сохранить в Printing Defaults каждой очереди. `print_dpi=600` задаёт
метаданные готового JPEG, но приложение больше не подменяет настройки драйвера.

### Точные настройки DS-RX1HS

Официальный Windows 10 driver bundle уже лежит в
`drivers/DRIVER_RX1HS_WIN_10-3.zip`. Внутри находятся установщик, файл
`DSRX1.gpd` и инструкция `DS-RX1_PrinterDriver_Win10_V1.13.pdf`. Таблица
**2.5 Print Area** различает две формы, которые окно Windows показывает как
`6x4` и `4x6 (PR)`:

| Paper Size | Orientation | Растр при 600x600 DPI |
| --- | --- | --- |
| `(6x4)` | `Portrait` | `3688x2480` — формат файлов фотобудки |
| `PR (4x6)` / `4x6 (PR)` | `Portrait` | `2480x3688` — вертикальная форма |

`PR` здесь означает отдельную portrait-first форму драйвера, а не качество и
не borderless. Для всех создаваемых фотобудкой горизонтальных листов, включая
grid, две strips и Telegram-печать, в обеих очередях должны быть сохранены:

- **Orientation:** `Portrait`;
- **Paper Size:** `(6x4)`, не `4x6 (PR)`;
- **Print Quality:** `600x600 DPI`;
- **Border:** `Disable` (это и есть печать без белой рамки у DNP);
- **Copy Count:** `1`;
- в основной `DS-RX1` — **2inch cut:** `Disable`;
- в `DS-RX1 Strips` — **2inch cut:** `Enable`.

Слова `Portrait` и `(6x4)` выглядят нелогично для горизонтальной фотографии,
но именно это сочетание DNP документирует для растра `3688x2480`. Смешивание
`4x6 (PR)`, `Landscape` и горизонтального файла создаёт несовпадающую геометрию
страницы и может дать белый лист либо узкую полоску изображения у края.

В Windows Printing Preferences этого пользователя один раз нужно проверить
borderless/margin off, glossy или matte и, если нужна автоматическая резка
полосок, включить `2inch cut` на отдельной очереди `printer_name_strips`.
Драйвер и private DNP-настройки могут отличаться между администратором и
киоск-пользователем, поэтому тестовая печать из Проводника под администратором
не проверяет профиль `Photobooth`.

## Переподключение камеры

Если Canon отключается по USB, приложение немедленно прерывает текущую съёмку,
очищает незавершённое видео и команды камеры и показывает экран поиска.
Частичная сессия остаётся локально для диагностики, но не попадает в очередь
печати или Яндекс Диска. После подключения камера настраивается заново, и будка
сама возвращается на главный экран без тапа и перезапуска приложения. Уже
завершённые сессии в очереди Яндекс Диска при этом не затрагиваются.

EDSDK и COM инициализируются один раз на весь camera worker. При USB-обрыве
закрывается и переоткрывается только camera session/reference; глобального
`EdsTerminateSDK` между попытками нет. Поиск повторяется с паузами 2, 4, 8 и
далее 10 секунд.

## Live view и безопасная область экрана

Canon отдаёт через EDSDK отдельный EVF-JPEG: backend передаёт эти байты в
`/live` без ресайза и кропа. Это не полноразмерный снимок `jpeg_large_fine`
(для EOS R8 обычно 6000×4000), а уменьшенный поток с разрешением, выбранным
самой камерой. При первом кадре каждой сессии точный размер записывается в лог,
например `Canon EVF JPEG: 960x640`; это же разрешение становится базовым для
session-видео.

Frontend по умолчанию использует `live_view_fit: "contain"`: весь EVF-кадр
центрируется без CSS-кропа, свободная область остаётся чёрной. Прежний режим
`cover`, заполняющий весь монитор с обрезкой краёв, доступен через
`_live_view_fit_options`. `live_view_margin_top_percent` и
`live_view_margin_bottom_percent` задают безопасные чёрные поля для частей
монитора, закрытых деревянной рамкой; сейчас установлено по 5%. На экране
высотой 1080 это примерно по 54 пикселя. Поля можно настраивать независимо от 0
до 25, не меняя код. `mirror_live_view` только зеркалит изображение по
горизонтали и не влияет на масштаб или кроп.

Для длительной автономной работы приложение при каждом открытии EDSDK-сессии
отключает Canon Auto power off и раз в 60 секунд продлевает shutdown timer.
Property events сразу сообщают об изменениях питания и температурных
ограничений, а раз в 10 минут в лог записываются также AE mode, доступные кадры
и свободное место. Новая сессия не запускается, если на диске фотографий меньше
`min_free_disk_gib` из `config_camera.json`. Пока `app.py` работает в Windows,
системный sleep и отключение дисплея также блокируются через process-scoped
execution-state request; после выхода приложения запрос снимается.

## Киоск-режим

`_setup_windows.bat` (от админа):
1. Создаёт/проверяет `python/` (через `_ensure_python.bat`)
2. Создаёт пользователя Photobooth с пустым бессрочным паролем
3. Shell = `python\pythonw.exe app.py` (вместо explorer.exe)
4. Автологин без пароля

Выход: Ctrl+Alt+Del → сменить пользователя.
Откат: `_undo_setup.bat`

## Облако

Основной transport — публичный REST API Яндекс Диска с OAuth-токеном.
Перед ивентом отдельная папка задаётся Telegram-командой `/event Название`.
После подтверждения команда атомарно обновляет `yadisk_folder` в конфигурациях
будки и VPS.
При создании event будка загружает встроенную рекламную карточку
`assets/yadisk/photobooth_card.jpg` в
`<event>/000_фотобудка_контакты.jpg`. После публикации QR каждой новой
session-папки эта же карточка копируется туда в фоне средствами Яндекс Диска.
Точный MD5 делает операции идемпотентными: совпадающий файл не передаётся
повторно, а заменённый локальный ассет обновится в корне и во всех последующих
сессиях. Карточка не входит в `session_ready`, поэтому VPS не принимает её за
гостевое фото и не отправляет в медиаподборке.
Оригиналы фото и видео доступны одновременно в двух представлениях:

```text
/
  /<event>/
    000_фотобудка_контакты.jpg
    20260722_143205_<session>_photo_01.jpg
    ...
    20260722_143205_<session>_video.mp4
  /<event>_by_sessions/
    /0000_print_jobs/
    /2026-07-22_14-32-05_<session>/
      000_фотобудка_контакты.jpg
      photo_01.jpg ... photo_04.jpg
      video.mp4
```

Коллекция `<event>_by_sessions` — сосед обычного event, а не его подпапка.
Поэтому гости, которым позже расшарят плоский `<event>`, не увидят дубликаты.
Сразу после получения четырёх кадров будка создаёт и публикует папку конкретной
сессии, пока параллельно идёт выбор шаблона и кодирование видео. Прямой
`public_url` Яндекс Диска подготавливается заранее; промежуточного
сайта/редиректа нет. Файлы появляются по этой стабильной ссылке по мере загрузки.

QR скрыт во время съёмки, обработки превью и выбора шаблона. Он может появиться
на следующем экране печати, как только готова публичная ссылка, и затем остаётся
на главном экране до начала следующей сессии. Пока медиа ещё передаются, подпись
просит гостя немного подождать и обновить страницу. `done_screen_seconds`
(сейчас 8 секунд) задаёт длительность отдельного экрана печати, а не срок жизни
QR.

Облачная доставка не зависит от выбора шаблона. Сразу после скачивания всех
кадров запускаются кодирование видео, подготовка QR и фоновая задача доставки;
последняя ждёт только готовности `session.mp4`, после чего сохраняет фото и
видео в durable outbox. Крестик на экране выбора пропускает компоновку и печать,
не списывает разрешённую `/unblock`-сессию и сразу возвращает экран в `idle`.
Загрузка на Яндекс Диск и последующая отправка в Telegram при этом продолжаются
обычным способом.

Печатные `print_grid.jpg` и `print_strips.jpg` остаются только локальными файлами
для драйвера принтера. Они не входят в очередь, папку сессии, корень event или
манифест и не отправляются в Telegram.

Доставка сессии работает как durable outbox:

1. Будка заранее создаёт и публикует `<event>_by_sessions/<session>`, сразу
   отдаёт QR, затем в той же фоновой задаче серверно копирует туда
   `000_фотобудка_контакты.jpg`.
2. Четыре оригинала и видео сохраняются в локальную очередь `yadisk_queue.json`.
3. Все медиафайлы загружаются в папку сессии и проверяются только
   по размеру/видимости ресурса. Полного локального чтения больших медиа для
   MD5 больше нет. URL загрузки запрашивается с совместимым User-Agent клиента
   Яндекс Диска: обычным REST-клиентам Яндекс может ограничивать видео примерно
   до 128 KiB/s; этот же способ снятия лимита используют
   [`yadisk`](https://yadisk.readthedocs.io/en/stable/known_issues.html) и
   [rclone](https://github.com/rclone/rclone/commit/af9f8ced805509c3efd264fc5be2a82ea1a000f2).
4. Яндекс Диск серверно копирует файлы в плоский корень event. Дополнительной
   проверки size/MD5 копии нет; для ответа `202` ожидается только завершение
   операции копирования.
5. Последним загружается типизированное `session_ready` в стабильный
   `photobooth_system/control/to_vps`; оно содержит точный `event_folder`.
6. VPS скачивает плоские копии, проверяет их размер и отправляет в Telegram.
7. После успешной доставки VPS удаляет служебный JSON из `control/to_vps`;
   медиа не удаляются.

Приватная шина не зависит от активного event: будка раз в 10 секунд поллит
только `control/to_booth`, а VPS — только `control/to_vps`. В `to_vps` вместе
лежат `session_ready`, `command_response` и инициативные `booth_notice`, которые
VPS разводит по независимым asyncio workers. После успешной обработки служебные
JSON удаляются, а результат остаётся в логах и постоянных медиа.
Команды и ответы используют несовместимую control-схему v3. Каждая команда
обязана содержать типизированный адрес ответа
`reply_target: {"provider": "telegram|vk", "conversation_id": "..."}`;
будка валидирует его и возвращает нормализованный адрес в
`command_response`. Ответы и команды старой control-схемы v2 не принимаются,
поэтому обновление будки нужно установить до выкладки соответствующей версии
VPS. Версия `session_ready` меняется независимо и остаётся v2.
Результат выполненной команды сохраняется до подтверждённой отправки ответа:
если uploader Яндекс Диска дал timeout, следующий poll повторно отправляет тот
же `command_response`, не запуская очистку очереди или другое действие второй
раз.
Логи используют `control/logs`, обновления — `photobooth_system/updates`.

## Telegram-команды

- `/event Свадьба Ивановых 2026` — создать и активировать папку на будке и VPS;
  каждый `/event Кафе` также сбрасывает разрешённые сессии в `0`, блокирует
  старт и не публикует техническую папку через VPS;
- `/status` — state, камера, её health/последний disconnect, свободный диск,
  фактически применённая конфигурация камеры, event, очередь и версия будки;
- `/print_queue` — показать число заданий в Windows-spooler отдельно для
  основной (`grid`) и режущей (`strips`) очередей;
- `/clear_print_queue` — очистить обе Windows-очереди; ответ показывает
  количество заданий до очистки, удалённых и оставшихся;
- `/run` — запустить фотосессию;
- `/unblock [N]` — в техническом event `Кафе` задать точный остаток
  разрешённых фотосессий; без числа задаётся одна сессия, `0` снова блокирует;
- `/block` — понятный алиас для `/unblock 0`;
- `/logs`, `/clear_logs` — получить или очистить лог;
- `/get_config` — получить `photobooth_configs.txt`: сначала текущее runtime-
  состояние `cafe_unlock_state.json`, затем `config_app.json` и
  `config_camera.json`; отдельный `config_vps.json` приходит напрямую с VPS,
  без перезапуска будки;
- `/restart` — подтверждённый перезапуск;
- `/update` — вручную повторно опубликовать последний GitHub Release, если
  автоматический CI/CD нужно запустить в обход нового push; установка после
  `/restart`;
- `/link` — опубликовать активную event-папку и вернуть публичную ссылку;
- `/<поле> <значение>` — изменить существующее рабочее поле
  `config_camera.json`, например `/iso 200`, `/iso auto`,
  `/white_balance auto` или `/continuous_af false`. Будка проверяет тип и
  `_..._options`, атомарно сохраняет значение и после подтверждения делает
  штатный рестарт. Во время фотосессии команда отклоняется.

`av`, `tv` и `iso` проверяются напрямую по картам EDSDK `AV_MAP`, `TV_MAP` и
`ISO_MAP`, а не по JSON-списку, поэтому сохранить значение, которое камера потом
молча пропустит, невозможно. Telegram всегда присылает строку: `/iso 200`
сохраняется числом `200`, `/iso auto` — строкой `"auto"`, а `/iso 150`
отклоняется с перечислением допустимых значений. Синонимы приводятся к
каноническому виду карты: `/av 4` сохраняется как `"4.0"`, `/av f/5.6` — как
`"5.6"`, `/tv 1/2` — как `"0.5"`. `_av_options`, `_tv_options` и `_iso_options` в
`config_camera.json` перечисляют полные таблицы (28 значений Av, 52 Tv, 19 ISO
плюс `auto`); тест сверяет их с картами при каждой сборке. Реально доступный
диапазон при этом ограничен объективом и телом: недоступное значение EDSDK
отклонит, и `_set_prop_u32` запишет это в лог. Значение, которого нет в карте,
больше не игнорируется тихо — при настройке камеры в `photobooth.log` попадает
`ERROR Unsupported <поле>=...`. То же относится к именованным полям вроде
`white_balance` и `picture_style`: неизвестное имя раньше подменялось дефолтом,
теперь оно логируется и свойство не меняется.

Каждое публичное поле `config_camera.json` попадает ровно в одну из четырёх
категорий проверки, и отдельный тест падает, если появится поле без реальной
валидации:

- `av`, `tv`, `iso` — по картам EDSDK;
- поля с `_..._options` — по списку допустимых имён, регистр не важен
  (`/white_balance AUTO` сохранится как `auto`);
- `color_temperature`, `focus_delay`, `min_free_disk_gib` — по диапазону из
  `_..._range`: соответственно `2500..10000` с шагом `100`, `0..5` секунд и
  `0.25..512` GiB. Границы продублированы в конфиге и сверяются тестом;
- boolean — по написанию: `true/false`, `yes/no`, `on/off`, `1/0`, `да/нет`,
  `вкл/выкл`. Неоднозначное `2` или `maybe` отклоняется.

Тип сохраняется: `int` остаётся `int`, `float` — `float`, поэтому `/focus_delay 1`
запишется как `1.0`, а не как строка. Слишком большое или отрицательное число
отклоняется до записи файла — важно для `focus_delay`, которое camera worker
реально выжидает перед каждым кадром. Если такое значение всё же попало в конфиг
правкой вручную, при настройке камеры оно ограничивается границей диапазона и
пишется `ERROR Out-of-range <поле>=...`.

## Подтверждение применённой конфигурации камеры

Изменённое поле применяется не сразу: `config_camera.json` читается только в
момент настройки камеры, то есть при запуске приложения и при каждом USB-
переподключении. Поэтому `/<поле> <значение>` завершается штатным рестартом, а
ответ «Параметр камеры ... Перезапуск подтверждён» подтверждает только запись в
файл.

Фактический результат подтверждает отдельный readback. После настройки будка
перечитывает с камеры все 17 параметров, которые EDSDK отдаёт обратно (`ae_mode`,
`shutter_type`, `image_quality`, `av`, `tv`, `iso`, `white_balance`,
`color_temperature` при `white_balance=color_temp`, `picture_style`,
`color_space`, `drive_mode`, `af_mode`, `evf_af_mode`, `subject_tracking`,
`eye_detection_af`, `continuous_af`, `evf_view_type`, `disable_auto_power_off`),
и пишет их в `photobooth.log` строкой `Camera applied config: ...`. Host-only
поля, которых на камере нет (`focus_before_capture`, `focus_delay`,
`min_free_disk_gib`, `evf_keep_camera_screen`, `lock_camera_ui`,
`lock_mode_dial`), выводятся отдельной строкой `Camera host config: ...`.
Расхождение даёт `WARNING Camera config mismatch <поле>: requested=... actual=...`,
а свойство, которое камера вообще не сообщила — `WARNING Camera config
unavailable <поле>`.

Тот же отчёт доступен двумя способами без чтения логов:

- при каждой успешной настройке камеры будка сама отправляет его администратору.
  Отчёт публикуется как типизированный `booth_notice` в
  `photobooth_system/control/to_vps`, а доставляет его в Telegram или ВК уже VPS:
  у будки нет ни `TG_BOT_TOKEN`, ни адреса администратора, поэтому `booth_notice`
  не содержит `reply_target` и VPS использует собственный настроенный адрес.
  Настройка происходит при запуске приложения и при USB-переподключении, поэтому
  отчёт приходит в обоих случаях одинаково. Неудачная доставка не мешает работе
  будки. В `to_vps` хранится не более 20 непрочитанных notice: самые старые
  удаляются по метке времени в имени;
- `/status` показывает секцию `Applied config` с тем же содержимым, поэтому
  подтверждение можно запросить в любой момент после рестарта.

В обоих случаях каждая строка помечена: `✓` — камера подтвердила значение из
конфига, `✗` — камера оставила другое (значение из конфига показано рядом),
`?` — камера не сообщила свойство. Отдельные строки `НЕ ПРИМЕНИЛОСЬ` и
`Камера не сообщила` перечисляют проблемные параметры, а при полном совпадении
выводится «Все параметры применены камерой без расхождений».

Расхождение обычно означает не ошибку конфига, а ограничение железа: `_set_prop_u32`
перед записью сверяется с `EdsGetPropertyDesc`, и недоступное для текущего
объектива или тела значение отклоняется с `WARNING SetProp(...) skipped`.
Например `av` вне диапазона установленного объектива останется прежним, хотя
в `config_camera.json` он сохранён корректно.

## Восстановление Canon R8

На экране `ИЩЕМ КАМЕРУ` есть кнопка `ПЕРЕПОДКЛЮЧИТЬ КАМЕРУ`. Она доступна только
вне фотосессии и выполняет один последовательный сценарий: останавливает поток
EDSDK, запускает системный PnP-reset только для USB ID Canon R8
`VID_04A9/PID_330C`, ждёт пять секунд и запускает новый поток EDSDK. Та же
операция вызывается кнопкой через локальный HTTP POST `/api/camera/recover`;
отдельной удалённой команды для неё нет.

Привилегированная часть хранится как неизменяемое действие Task Scheduler, а
пользователь `Photobooth` получает право только запускать эту задачу. Если задача
ещё не установлена, кнопка всё равно чисто перезапустит EDSDK и прямо сообщит,
что для аппаратного USB-reset нужно один раз повторно выполнить
`_setup_windows.bat` от администратора.

При обычной остановке будка теперь проверяет, завершился ли EDSDK-поток, до трёх
раз повторяет `EdsCloseSession` только для временных Canon busy-ошибок и пишет в
`/status` реальный результат последней очистки. Необязательный `UI lock` выполняется
последним, после записи и readback всех свойств. Поэтому
`EDS_ERR_DEVICE_BUSY` на этой команде не выдаётся за потерю камеры: максимум
останутся доступными кнопки корпуса, а открытая сессия продолжит работать.

При каждом переключении на event `Кафе` остаток сначала атомарно сбрасывается в
`0`; если сохранить блокировку не удалось, event не меняется. Главный экран при
нулевом остатке показывает
`(ЗАБЛОКИРОВАНО)` и не принимает touch или `/run`. Команда `/unblock 5`
разрешает ровно следующие пять успешно завершённых фотосессий. Неудачная или
прерванная съёмка лимит не уменьшает. `/block` или `/unblock 0` обнуляет
остаток и блокирует следующий старт, не прерывая уже начатую сессию. Остаток
хранится отдельно в `cafe_unlock_state.json`, переживает рестарт и обновление
приложения; в любом другом event будка всегда разблокирована.

Локальный лог состоит из активного `photobooth.log` и одного предыдущего
`photobooth.log.1`, по 200 KB каждый. `/logs` склеивает их в хронологическом
порядке и присылает одним файлом. `/clear_logs` полностью обнуляет активный лог,
удаляет все ротационные backup-файлы и продолжает запись с чистого состояния.
Поэтому следующий `/logs` содержит только события, произошедшие уже после
очистки (включая подтверждение самой команды `clear_logs`).

Смена event отклоняется, пока идёт сессия или локальная загрузка. Сессию команда
не прерывает; после освобождения будки команду нужно повторить.

## Смена аккаунта Диска

Никаких ID ресурсов или аккаунта в коде нет. Нужно заменить `YADISK_TOKEN` в
`.env` будки и VPS, перезапустить оба процесса, выполнить `/event ...`, затем
`/update`. Системные папки создаются автоматически; данные старого аккаунта не
копируются.



## Печать

Что происходит:

- grid и strips сразу создаются горизонтальными 3688×2480;
- они без дополнительного поворота передаются штатному Windows print handler;
- Windows-очередь должна быть сохранена как (6x4) + Portrait + 600×600;
- DNP сам печатает этот горизонтальный растр на листе 15×10.

Для Telegram-фотографий подготовка отдельная:

- горизонтальная картинка остаётся как есть;
- вертикальная или квадратная поворачивается на 90° против часовой;
- `fit` пропорционально вписывается и центрируется на белом фоне;
- `fill` пропорционально заполняет лист с центральной обрезкой;
- в принтер всё равно уходит готовый горизонтальный JPEG.

Важно: настройки нужно сохранить в Windows, а не просто выбрать для одного задания:

- DS-RX1: (6x4), Portrait, 600×600, Border Disable, 2inch cut Disable;
- DS-RX1 Strips: всё то же, но 2inch cut Enable.


# Tests
- venv/bin/python -m unittest discover -s tests